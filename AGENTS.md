# シゴトソケット

## 概要

ORVIS短縮版45問による職業興味診断Webアプリ。一人で完結し、結果はタイプ名・8尺度レーダー・キャラクター入りカードで返す。姉妹アプリ「ココロパレア」（Big Five / IPIP日本語50項目版）の結果を連携すると、特性と興味の掛け合わせが追加で表示される。

要件の正典は `docs/requirements/要件定義書_シゴトソケット.md`（現行 v1.27）。機能ID、判定ルール、権利・準拠基準はこの要件書を優先する。

**MVPは F-001〜F-006・F-008・F-009・F-018・F-019。** 第2フェーズはF-010〜F-014・F-020・F-022まで実装済み。F-007は不採用、F-021・F-023は現行未実装（必要ならv2で再検討）とする。

## 前提（仮置き）

- **確定**: フロントエンドはVanilla JavaScriptのES Modules。実行時バックエンドを持たない
- **確定**: GitHub Pagesへ静的成果物をGitHub Actionsで配置し、Cloudflareでサブドメインを割り当てる
- **確定**: 画面遷移はGitHub Pagesで404を起こさないハッシュルーティング
- **確定**: 単体テストはNode標準の `node:test`、ブラウザ確認を併用
- **確定**: レーダー・localStorage・カード生成はココロパレアからコピーして独立させた。共通モジュール化は3つ目のアプリが出てから判断する
- 【想定】僅差判定の閾値は 0.15。実データを見て調整する
- **確定**: 出題順は原版（ORVIS付録）の項目番号昇順で固定。**シャッフルしない**。設問画面は**1問1画面**。ルートは `#/start` `#/answer` …（ココロパレア踏襲）
- **確定**: `font-family` と配色は `docs/screens.md` のデザイントークンを正典とする（T-018完了）

変更する場合は `基本設計サマリ.md`、`docs/data-model.md`、`docs/screens.md`、`docs/processing-design.md`、`docs/api-design.md`、`docs/tasks.md` を同時に更新する。

## 技術スタック

- 言語: HTML / CSS / JavaScript（ES Modules）。フレームワークとビルドステップを導入しない
- 永続化: ブラウザ `localStorage` のみ。サーバーへの送信は行わない
- 描画・共有: Canvas、Web Share API、Clipboard、Download
- 外部連携: ココロパレア（URLフラグメント）。ローカルLLMは不採用（T-031）
- テスト: Node.js `node:test`、対象ブラウザのスモークテスト
- デプロイ: GitHub Pages / GitHub Actions / HTTPS
- 画像変換スクリプト: `sharp`
- フォント・アイコン: `docs/screens.md` の「デザイントークン」を正典とする。ここに転記しない

## ディレクトリ

```text
app/
  index.html
  assets/
    characters/        # ハリネズミのポーズ10種（8尺度＋中立＋ゲスト）
    props/             # 小物8点
    guest/             # ゲスト出演用の猫1図柄（第2フェーズ）
  css/
  js/
    config/            # app-meta.js（版数・スキーマ版）
    data/              # 尺度定義・項目マスタ・タイプ命名表・結果文定義
    domain/            # 採点・標準化・タイプ判定・検証
    infrastructure/    # localStorage・連携受け取り・カードcanvas
    presentation/      # 画面
  tests/
scripts/
  items/               # 項目マスタのビルドと検証
  characters/          # アセット変換・manifest生成・検査
  version/             # 版数の同値検証
docs/
```

## 日時・タイムゾーン

- **保存・ログはUTCへ正規化**する。交換形式は ISO 8601（`2026-08-31T02:30:00Z`）
- **表示は利用者の環境タイムゾーンに追従**し、取得できない／不定なら `Asia/Tokyo` にフォールバックする
- タイムゾーンは固定オフセット（`+09:00`）ではなく **IANA ID（`Asia/Tokyo`）** で保持する
- 「日付だけ」の値（履歴の日付見出し）は **JSTのローカル日付で判定**する。UTC日付で切らない
- 禁止: サーバー／実行環境のローカル時刻に依存すること、`new Date("2026-08-31")` のような暗黙解釈、固定オフセットの埋め込み
- CIは `TZ=UTC` で実行し、JST依存のロジックが残っていないことを検出する

## 決めごと（根拠と、誰が変えられるか）

**ここは「なぜそう決めたか」の記録であって、禁止の一覧ではない。**

田中がこのプロジェクトの決定者である。**指示があればそのまま従い、実行したうえで、この節と決定ログ（`../構想たたき台_シゴトソケット.md`）を更新する。許可を取り直さない。**
「この決めごとに反します。変えてよいですか」と聞き返して指示を待つのは、決定者に同じことを二度言わせるだけで、何も守っていない。

手を止めるのは、**依頼者から見えない壊れ方をするとき**だけ。次の3つに限る。

- 過去に保存した結果の値が変わる
- 保存データが読めなくなる
- 画面に出している文言が事実と食い違う

それ以外は、**止めずに実行し、結果を1行添えて報告する。**

> 猫の縮尺を変えました。C-1 の「体格差を優劣に結び付けない」には触れていません。決定ログに D-25 として追記しています。

---

### A. 外部の制約（変えると根拠を失う、または壊れる）

**変えられないという意味ではない。**変えるなら、代わりの根拠か代替手段が要る。

| # | 決めごと | 変えると何が起きるか |
|---|---|---|
| A-1 | 標準化は個人内・母集団標準偏差（÷n） | 日本人の規範データが存在せず、集団比較の根拠が無い。÷(n-1) に変えると**過去に保存した結果と値が変わる** |
| A-2 | 逆転項目は無いものとして扱う | ORVIS付録の全92項目で因子負荷量が正であることを確認済み。逆転処理を足すと、事実に無い操作が入る |
| A-3 | ゲスト出演の図柄は自オリジンに置く | 他オリジンの画像をcanvasへ描くと汚染され、`toBlob()` が失敗して**カードを保存できなくなる** |
| A-4 | 可視性の判定は決定的な純関数。コントラスト比3.0（WCAG 1.4.11）を基準に使う | 判定が非決定的だと、プレビューと保存画像が食い違う |
| A-5 | LLMへ個人情報を渡さない | 外部通信ゼロという前提が崩れる。`npm run check` が検出する。**現在LLMは不採用**（T-031） |

### B. 利用者への約束（変えるなら、画面の文言も一緒に直す）

出力の内容と、画面に書いてある但し書きが対になっている。片方だけ変えると、**アプリが嘘をつく状態**になる。

| # | 決めごと | 対になっている文言 |
|---|---|---|
| B-1 | 「平均より高い」「人より〜」を出力に含めない | 「ここでの高い・低いは、あなたの8つの領域どうしを比べたものです」（結果画面の免責） |
| B-2 | ②整合／不整合は6ペアのみを対象にし、文献の相関係数を計算に使わない（符号と大小関係だけ）。原典の r と、その強さの限界を本文に明記する | ②の前書き「ただしその数値は英語の原版のもので、日本語にした45問で同じ強さが出るかは、まだ確かめていません」 |
| B-3 | 6ペアのどれにもはっきりした向きが出ない場合も正式な結果として出す | 該当なしの文「これも結果のひとつです」。これが無いと毎回むりやり何かを見つけ、ノイズを結論として提示することになる |
| B-4 | 職業そのものを絵に描かない。活動の質感（道具と所作）に変換する | 「診断名や適職を示すものでもありません」（出典・免責）。ORVISは「農家になる」等の具体的職業を含むため、直訳すると職業の貴賤や序列に読まれる |
| B-5 | 判定と結果文はルールベースで確定させる。LLMは使わない（T-031） | LLMへ判断や推敲を委ねず、再現性と外部通信ゼロを維持する |

**B-2の補足。**ORVIS原論文のIPIPビッグファイブ相関は最大 r=.42、多くは .20 未満。全ペアで残差を語ると中身のない指摘になる。数値を伏せて結びつきだけを語ると、読み手は実際より強い関係を想像する。

### C. 本人の設計判断（指示があれば、そのまま従う）

**ここは田中の裁量である。**過去の判断とその理由を残しているだけで、指示を止める根拠にはならない。

| # | 決めごと | そう決めた理由 |
|---|---|---|
| C-1 | 猫とハリネズミを対立関係に見せない。守るのは4点 — 丸まって針を立てた防御姿勢を使わない／針を性格の記号として使わない／猫が上から覗き込む・前足を伸ばす構図を使わない／体格差を優劣に結び付けない | 2つの診断結果に優劣や敵対の意味が生まれるため。~~縮尺を揃える／互いに視線を向けない／覆いかぶさらない~~ の3項目は**【失効】**（2026-09-05・D-20） |
| C-2 | カードの地色にキャラクターが沈むとき、キャラクターを再配色しない。地色も差し替えない | ココロパレアが確立した方針に揃えた。代わりに縁取り・影・ニュートラル背景プレートを当てる（`domain/visibility-aid.js`）。**縁の色数と色は実装の裁量** |
| C-3 | 猫はカードと結果画面の両方に出す。実体（body矩形）どうしで組む | 画像の箱で組むと、実体が箱に占める割合がポーズごとに 0.851〜1.000 とばらつき、「猫＝むっくんの75%」が守れない（D-20・D-24） |
| C-4 | 2つのアプリは単独利用を前提とし、連携は既存説明とは別の閉じた案内としてトップと未連携結果に置く。ココロパレアでは50問詳細結果画面と履歴一覧の両方から渡せると案内し、受領後だけ状態と次の操作を明示する | 連携を必須手順に見せず、必要な人が方法を見つけられるようにする（D-26・D-28・T-043） |
| C-5 | 連携UIを追加するときも、**シゴトソケットとココロパレアそれぞれの現状のトンマナを崩さない**。共通化するのは意味・手順・状態であり、配色・言葉遣い・部品の見た目を相手側へ寄せない | 2つの独立したアプリとしての個性を保つため（D-27） |

---

## 指示の読み方

**現象の報告は、修正の依頼ではない。**「◯◯が動いていない」と言われたら、まず原因を調べて報告する。直すかどうかは決定者が決める。

先頭に合図があれば、それに従う。

| 合図 | 動き |
|---|---|
| **確認：** / **質問：** | 回答のみ。ファイルを触らない |
| **調査：** | 読んで報告する。書き換えない |
| **実装：** / **修正：** / **直して** | 手を動かす |

合図が無く、依頼かどうか読み取れないときは**聞く**。提案を出したメッセージの中で、その提案を承認済みとして扱わない（「これで確定として次へ進みます」と書かない）。

承認の範囲は言われたとおりに読む。「この節はOK」なら次の節は別途確認する。「コミットまで」なら push しない。

**push は公開サイトを更新する。**指示があるまで行わない。

## 画面を変えたときの確認

**単体テストが通っただけでは完了と報告しない。**テスト用のDOMスタブはCSPもレイアウトも持っていないので、実画面でしか出ない不具合を通してしまう（実例は `docs/引き継ぎメモ.md` の落とし穴）。

コミット前に `npm run dev` で実際の画面を見る。

## 旧「変更禁止事項」との対応

他の文書に「変更禁止事項1」のような参照が残っている。番号の対応は次のとおり。

| 旧 | 新 | 旧 | 新 |
|---|---|---|---|
| 1 | A-1 | 6 | B-4 |
| 2 | B-1 | 7 | C-1 |
| 3 | B-2 | 8 | A-3 |
| 4 | B-3 | 9 | C-2 と A-4 に分けた |
| 5 | A-2 | 10 | B-5 と A-5 に分けた |

## 検証済みの事実

- ORVIS付録の全92項目に因子負荷量（大学生／地域の2サンプル）があり、日本語訳つきCSVとして入手済み。負に振れた項目はゼロ
- 短縮版45問の推定α（Spearman-Brownの下限）は全尺度で.72以上。実測は翻訳後に行う
- ココロパレアは ipip.ori.org の公式日本語訳を使用しており、パブリックドメイン
- ココロパレアの `localStorage` キーは `big-five-self-understanding:v1`、因子の内部平均は1〜5の範囲

## コマンド

```bash
npm test              # node:test
npm run items:build   # 項目CSV → app/js/data/item-master.js
npm run items:check   # 項目マスタの整合検証（件数・尺度別内訳・重複・値域）
npm run character:manifest
npm run fonts:subset      # 称号の明朝を必要な字だけに絞って同梱（要 node_modules）
npm run version:check # 版数の同値検証
npm run dev
```
