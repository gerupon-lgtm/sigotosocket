# シゴトソケット

## 概要

ORVIS短縮版45問による職業興味診断Webアプリ。一人で完結し、結果はタイプ名・8尺度レーダー・キャラクター入りカードで返す。姉妹アプリ「ココロパレア」（Big Five / IPIP日本語50項目版）の結果を連携すると、特性と興味の掛け合わせが追加で表示される。

要件の正典は `docs/requirements/要件定義書_シゴトソケット.md`（v1.4）。機能ID、判定ルール、権利・準拠基準はこの要件書を優先する。

**MVPは F-001〜F-006・F-008・F-009・F-018・F-019。** 連携（F-007・F-010〜F-014・F-020〜F-023）は第2フェーズ。

## 前提（仮置き）

- 【想定】フロントエンドはVanilla JavaScriptのES Modules。実行時バックエンドを持たない
- 【想定】GitHub Pagesへ `app/` の静的成果物をGitHub Actionsで配置し、Cloudflareでサブドメインを割り当てる
- 【想定】画面遷移はGitHub Pagesで404を起こさないハッシュルーティング
- 【想定】単体テストはNode標準の `node:test`、ブラウザスモークを併用
- 【想定】共通処理（レーダー・localStorage・カード生成・LLM連携）はココロパレアからコピーして独立させる。共通モジュール化は3つ目のアプリが出てから判断する
- 【想定】僅差判定の閾値は 0.15。実データを見て調整する
- **確定**: 出題順は原版（ORVIS付録）の項目番号昇順で固定。**シャッフルしない**。設問画面は**1問1画面**。ルートは `#/start` `#/answer` …（ココロパレア踏襲）
- 【想定】`font-family` と配色は暫定（`docs/screens.md` のデザイントークン）。実画面のモック比較で見直す

変更する場合は `基本設計サマリ.md`、`docs/data-model.md`、`docs/screens.md`、`docs/processing-design.md`、`docs/api-design.md`、`docs/tasks.md` を同時に更新する。

## 技術スタック

- 言語: HTML / CSS / JavaScript（ES Modules）。フレームワークとビルドステップを導入しない
- 永続化: ブラウザ `localStorage` のみ。サーバーへの送信は行わない
- 描画・共有: Canvas、Web Share API、Clipboard、Download
- 外部連携: ココロパレア（URLフラグメント）、自宅ローカルLLM（推敲のみ・任意）
- テスト: Node.js `node:test`、対象ブラウザのスモークテスト
- デプロイ: GitHub Pages / GitHub Actions / HTTPS
- 画像変換スクリプト: `sharp`
- フォント・アイコン: `docs/screens.md` の「デザイントークン」を正典とする。ここに転記しない

## ディレクトリ

```text
app/
  index.html
  assets/
    characters/        # ハリネズミのポーズ8種
    props/             # 小物8点
    badges/            # 記号19点（第2フェーズ）
    guest/             # ゲスト出演用の猫1図柄（第2フェーズ）
  css/
  js/
    config/            # app-meta.js（版数・スキーマ版）
    data/              # 尺度定義・項目マスタ・タイプ命名表・結果文定義
    domain/            # 採点・標準化・タイプ判定・検証
    infrastructure/    # localStorage・LLMクライアント・カードcanvas
    presentation/      # 画面
  tests/
scripts/
  items/               # 項目マスタのビルドと検証
  characters/          # アセット変換・manifest生成・検査
  version/             # 版数の同値検証
docs/
```

## 日時・タイムゾーン

- **保存・ログはUTCへ正規化**する。交換形式は ISO 8601（`2026-08-31T02:30:00Z`）
- **表示は利用者の環境タイムゾーンに追従**し、取得できない／不定なら `Asia/Tokyo` にフォールバックする
- タイムゾーンは固定オフセット（`+09:00`）ではなく **IANA ID（`Asia/Tokyo`）** で保持する
- 「日付だけ」の値（履歴の日付見出し）は **JSTのローカル日付で判定**する。UTC日付で切らない
- 禁止: サーバー／実行環境のローカル時刻に依存すること、`new Date("2026-08-31")` のような暗黙解釈、固定オフセットの埋め込み
- CIは `TZ=UTC` で実行し、JST依存のロジックが残っていないことを検出する

## 変更禁止事項（根拠つき）

次はいずれも検討の末に確定した事項。「改善」として書き換えない。変更が必要なら要件定義書の改訂から行う。

1. **標準化は個人内・母集団標準偏差（÷n）** — 日本人規範データが存在しないため集団比較ができない。標本標準偏差（÷n-1）に変えると過去の結果と値が変わる
2. **「平均より高い」「人より〜」という表現を出力に含めない** — 上記の理由。個人内比較しか根拠がない
3. **②整合／不整合の判定は6ペアのみ。文献の相関係数を計算に使わない** — ORVIS原論文のIPIPビッグファイブ相関は最大 r=.42、多くは.20未満。全ペアで残差を語ると中身のない指摘になる。日本語訳と短縮で厳密な対応も失われるため、符号と大小関係だけを使う
4. **「ズレが見つからなかった」を正式な結果として出す** — これがないと毎回むりやり何かを見つけ、ノイズを結論として提示することになる
5. **逆転項目は存在しない** — ORVIS付録の全92項目で因子負荷量が正であることを確認済み。逆転処理を足さない
6. **職業そのものを絵に描かない** — ORVISは職業興味尺度で「農家になる」等の具体的職業を含む。直訳すると職業の貴賤や序列に読まれる。活動の質感（道具と所作）に変換する
7. **猫とハリネズミを対立関係に見せない** — 縮尺を揃える／互いに視線を向けない／覆いかぶさらない。加えて**丸まって針を立てた防御姿勢を使わない**（拒絶・閉じこもりに読まれ「心を閉ざしている」という含意が付く）。**針を性格の記号として使わない**（造形であって性格ではない）。2つの診断結果に優劣や敵対の意味が生まれるため
8. **ゲスト出演の図柄は自オリジンに置く** — 他オリジンの画像をcanvasへ描くと汚染され `toBlob()` が失敗し、カードを保存できなくなる
9. **カードの地色にキャラクターが沈むとき、キャラクターを再配色しない。地色も差し替えない** — ココロパレアが確立した方針。代わりに縁取り・影・ニュートラル背景プレートを決定的に適用する（`domain/visibility-aid.js`）。判定は純関数なので、プレビューと保存画像が食い違わない。**縁の色数と色そのものは実装の裁量とする**（白のみ、明暗二重、クリームを混ぜる等）。守るのは「再配色しない」「地色を差し替えない」「判定が決定的である」「コントラスト比3.0（WCAG 1.4.11）を判定基準に使う」の4点であり、縁の構成はそこに含まれない（2026-09-05改訂）
10. **LLMは言い回しの調整のみ。判定はルールベースで確定させ、個人情報を渡さない** — LLMに判定させると再現性が失われる。LLM停止時はアプリ側で組み立てた文をそのまま出す

## 検証済みの事実

- ORVIS付録の全92項目に因子負荷量（大学生／地域の2サンプル）があり、日本語訳つきCSVとして入手済み。負に振れた項目はゼロ
- 短縮版45問の推定α（Spearman-Brownの下限）は全尺度で.72以上。実測は翻訳後に行う
- ココロパレアは ipip.ori.org の公式日本語訳を使用しており、パブリックドメイン
- ココロパレアの `localStorage` キーは `big-five-self-understanding:v1`、因子の内部平均は1〜5の範囲

## コマンド

```bash
npm test              # node:test
npm run items:build   # 項目CSV → app/js/data/item-master.js
npm run items:check   # 項目マスタの整合検証（件数・尺度別内訳・重複・値域）
npm run character:manifest
npm run fonts:subset      # 称号の明朝を必要な字だけに絞って同梱（要 node_modules）
npm run version:check # 版数の同値検証
npm run dev
```
