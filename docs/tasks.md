# 実装タスク — シゴトソケット

## 進め方（2026-09-04 合意）

**一度に1つだけ閉じる。閉じてから次へ進む。**要素とレイアウト、仕様と実装を混ぜない。
中断しても戻れるよう、閉じた時点でこの節の状態を更新する。

| 段 | 内容 | 状態 |
|---|---|---|
| ① | **カードに載る要素の確定** | **完了（2026-09-04）** |
| ② | カードのレイアウト確定（座標・余白・文字被り） | **進行中** |
| ③ | 実装（ホランド型 → キャラクター組み込み → フォント同梱） | **完了（2026-09-05）**。ホランド型・キャラクター組み込み・フォント同梱・目録まで閉じた |

### ① の判断

1. ~~ホランド型をカードに載せるか~~ → **閉じた（2026-09-04）**。独立した1行として載せる。**常に1行**で、結果によって情報量を変えない。1位が「言葉」のときは「6類型の外にある領域」と1行で出し、2位の型は結果画面のみ（要件定義書 §7-3、決定ログ D-18）
2. ~~第2フェーズの2要素の場所を確保するか~~ → **閉じた（2026-09-04）**。**4:5（1080×1350）を維持したまま、約100pxの帯を確保する**。捻出はレーダーの半径を143→130に縮めて行う。MVPの間はそこを余白として使う

   - 根拠: ココロパレアの実装（1080×1800）を実測し、繰り返し要素のピッチが45px（因子バー）と122px（枠付きカード）と判明。バッジ1〜2点＋称号名1行はその中間で約100px
   - ~~F-022のゲスト猫は**3枚目の合成カード**であり、この帯には不要~~ → **【失効】**（2026-09-05・D-20）。3枚目は作らず、同じカードにゲスト猫を描く。ただし猫はキャラクター帯に置くため、この帯が不要である点は変わらない

### ② の絶対条件

**文字被りは一切許容しない。** 目視確認ではなく機械的に検証する。

1. レイアウトを座標データとして1箇所に定義し、カード描画はそこからのみ行う（描画側に座標を直書きしない）
2. テストで**箱どうしが交差しないこと**を検証する
3. テストで**実際の文字列が箱に収まること**を検証する。対象は称号28件・中立副題28件・8領域名・ホランド型7種・全固定文言
4. 幅の上限は「和文の文字数 × フォントサイズ」で見積もる（和文は原則全角のため）。ラテン文字・数字は半角として別に数える。実行環境の書体差を吸収するための安全側の見積もり

### 混ぜて進めて失敗した記録

アイコン検討 → B案（ヘッダーにマーク）→ **①を閉じないままレイアウトのモックを作成** → 配色・地色（②寄り）→ ホランド型・文言（①）と往復した。
結果、座標が決まったモックに後から要素を挿し込む形になっている。要素が固まる前にレイアウトを作らない。


## 進捗（2026-09-04）

| タスク | 状態 |
|---|---|
| T-001 プロジェクト初期化 | **完了**。git初期化済み、リモート `origin` 設定済み（https://github.com/gerupon-lgtm/sigotosocket）。**push未実施** |
| T-002 版数管理 | **完了**（`npm run version:check`） |
| T-003 項目マスタのビルド | **完了**（`npm run items:build` / `items:check`） |
| T-004 尺度マスタとタイプID | **完了** |
| T-005 localStorage 基盤 | **完了** |
| T-006 ルーティングと画面骨格 | **完了** |
| T-007 S-001 トップ | **完了**（2026-09-05 にトンマナ改訂）。本文を白い角丸パネルに載せ、閉じたままの説明パネル（ツール／連携でできること／むっくん）を追加。ココロパレアの `.start-main-panel` と `.start-introduction` に倣う |
| T-008 S-002 設問（1問1画面） | **完了** |
| T-009 採点・個人内標準化 | **完了** |
| T-010 タイプ判定・僅差 | **完了** |
| T-011 8尺度レーダー | **完了** |
| T-012 結果文の組み立て | **完了** |
| T-013 S-003 結果画面 | **完了** |
| T-014 出典・免責・データ削除 | **完了** |
| T-015 キャラクターアセット | **完了**（2026-09-05）。ポーズ10種＋小物8点を `npm run character:build` で1024×1024のwebpへ変換し組み込み済み。`character:check` 通過。`npm run character:manifest` で目録（characterId / imagePath / alt / 寸法 / sha256）を生成。altは `scripts/characters/character-alt.js` が手書きの正典。カードの画像パスと読み上げ文は目録から引く |
| T-016 カード生成 | **完了**（2026-09-05にレイアウト全面刷新）。1080×1800、座標は `app/js/presentation/card-layout.js` が正典。文字被りを `card-layout.test.js` が機械的に検査する。判定不能のときは中立ポーズを描く（小物は無し）。「準備中」の枠が出るのはアセットの読み込みに失敗したときだけ |
| T-017 S-004 カード画面・共有 | **完了** |
| T-018 フォント・アイコンの確定 | **完了**（2026-09-05）。画面の書体はココロパレアと同じ並び（`"Sawarabi Gothic", "Hiragino Kaku Gothic ProN", "Yu Gothic", "Meiryo", system-ui`）。**同梱も外部読み込みもしない**ので外部通信ゼロを保つ。カードの書体も同社に合わせた（`docs/brand/card-layout.md`）。Noto Serif JP（SIL OFL 1.1）を明朝で描く**161字**に絞って同梱（38.9KB）。`npm run fonts:subset` で生成、`@font-face` は `style.css`、CSPへ `font-src 'self'` を明示。許諾全文は `app/assets/fonts/OFL.txt`、出典は S-005 に表示 |
| T-019 タイプ命名表28件 | **完了**（本人レビュー反映済み。決定ログ D-15） |
| T-020 端値テスト | **完了** |
| T-021 PC・スマホのスモーク | **実機確認を実施（2026-09-05・Pixel 6a とPC）。**指摘4件は対応済み（結果画面の称号／URL共有／トップへの導線／ヘッダー）。以下はその前の記録。**ブラウザ幅での確認は完了**（2026-09-05）。375×812 と 1280×900 で S-001→S-002→S-003→S-004 を完走。横スクロール 0px、選択肢のタップ領域は最小53px、`toBlob()` は 478KB のPNGを返す。判定不能の経路も確認。**実機（実端末）での確認は未実施。**エミュレートした画面幅であって実機ではない。手順は `docs/実機確認手順_T-021.md`（Pixel 6a / Chrome とPC） |
| T-022 外部通信ゼロの検証 | **完了**（`npm run check`） |
| T-023 日時・タイムゾーンの検証 | **完了**（CIで TZ=UTC / Asia/Tokyo の両方を実行） |
| T-024 ホランド型の表示（F-024） | **完了**（2026-09-05）。`app/js/domain/holland.js`（テスト9件）を `result-screen.js` から呼び、結果画面に「ホランド型」節として出す。順位が無い（判定不能）ときは節ごと出さない。画面側のテスト3件を追加 |
| 第2フェーズ（T-025〜T-032・T-035） | **T-025・T-026・T-028・T-030・T-035・T-027の③が完了（2026-09-05）。**②はデータ待ち。残りは T-029（バッジ）・T-032（ココロパレア側）。**T-031 は不要と決定** |

テスト185件がTZ=UTC・TZ=Asia/Tokyoの双方で通過。



上から順に着手する。各タスクは完了条件を満たし、検証方法を通ってから次へ進む。

**前提（仮置き）**: 僅差閾値0.15、`font-family`、LLMタイムアウト3秒は【想定】。実装中に変わり得る。
**確定**: 出題順は原版の項目番号昇順で固定（シャッフルなし）。設問画面は1問1画面。いずれもココロパレア踏襲。

## MVP（第1リリース）

### T-001 プロジェクト初期化
- **完了条件**: 新規リポジトリに `app/`（`index.html`・`css/`・`js/{config,data,domain,infrastructure,presentation}`）と `scripts/`・`docs/`・`tests/` が存在し、`npm test` `npm run dev` が動く。GitHub Actionsで `app/` をGitHub Pagesへ配置できる
- **検証方法**: `npm test` が0件でも成功する／ローカルで `#/` が表示される／Actionsが緑になり、Pages URLで表示される／Cloudflareでサブドメインを向けた状態でHTTPSアクセスできる
- **対応**: 非機能（運用・デプロイ）

### T-002 版数管理（F-009）
- **完了条件**: `app/js/config/app-meta.js` に `appVersion`（`v0.1.0`）・`storageSchemaVersion`・`itemSetVersion`・`scoringVersion`・`typeRuleVersion`・`characterManifestVersion`・`cardTemplateVersion` を定義。フッターに `appVersion` を表示。`npm run version:check` が全反映先の同値を検証する
- **増分規則**: MINOR更新時はPATCHを0、MAJOR更新時はMINOR/PATCHを0へ戻す。正典は `app-meta.js`
- **検証方法**: `app-meta.js` の値を1つずらすと `version:check` が失敗する／フッターの表示が `app-meta.js` と一致する
- **対応**: F-009、非機能（運用）

### T-003 項目マスタのビルド（F-001の前提・正確性）
- **完了条件**: `ORVIS短縮版45問_選定リスト.csv` から `npm run items:build` で `app/js/data/item-master.js` を生成する。**手書きしない**。並び順は**原版の項目番号昇順**（シャッフルなし）。`npm run items:check` が件数45・尺度別内訳（5/4/6/6/4/7/6/7）・itemId重複なし・textJa空なし・負荷量の値域・**順序が項目番号昇順であること**を検証する
- **検証方法**: CSVの1行を削って `items:check` が失敗する／生成物をgit管理し、再生成しても差分が出ない（決定的生成）／ローカライズ済み3項目に `localized: true` が付く
- **対応**: 非機能（正確性・準拠基準）

### T-004 尺度マスタとタイプID（F-004の前提）
- **完了条件**: `scale-definitions.js`（8件・正準順）と `type-definitions.js` のID28件が存在する。IDは `type-<a>--<b>` の正準順。名前・説明は T-019 で埋める
- **検証方法**: 28件が `C(8,2)` と一致する／ID生成が正準順であることのテスト／同じ2尺度から順序を変えて呼んでも同一IDが返る
- **対応**: F-004

### T-005 localStorage 基盤（F-002）
- **完了条件**: キー `sigotosocket:v1` の envelope 読み書き。`schemaVersion` 不一致は読み捨てず保持し、移行不能を表示する。書込失敗（容量超過・プライベートモード）時はメモリ動作へフォールバックし、その旨を表示する。全削除操作を提供する
- **検証方法**: `localStorage` を投げるスタブで例外が外へ漏れない／`schemaVersion: 999` の envelope を置いてもデータが消えない／全削除後に初回状態へ戻る／保存直後にリロードして回答が復元される
- **対応**: F-002、非機能（セキュリティ）

### T-006 ルーティングと画面骨格
- **完了条件**: `#/start`・`#/answer`・`#/result`・`#/card`・`#/about` のハッシュルーティング。未知のルートは `#/start` へ。**問番号をURLに持たない**（ココロパレア踏襲）
- **検証方法**: GitHub Pages上で各URLを直接開いて404にならない／ブラウザの戻るで前画面へ戻る／未知のハッシュが `#/start` へ正規化される
- **対応**: 全画面

### T-007 S-001 トップ（F-002・F-008・F-009）
- **完了条件**: 所要時間「約5分・45問」を明示。保存データがあるときのみ「続きから」「前回の結果を見る」を出す。**ビッグファイブの有無を聞かない**。免責要約と `#/about` 導線、フッターに版数
- **検証方法**: 保存データなしで「続きから」が出ない／途中保存ありで「続きから」が正しいページへ戻る／完了済み結果ありで結果画面へ飛べる
- **対応**: F-002、F-008、F-009

### T-008 S-002 設問画面（F-001・F-002）
- **完了条件**: 45問を**1問1画面**で出題。5件法。進捗は `n / 45問`。「戻る」は1問目でのみ無効。回答のたびに保存。**出題順は原版の項目番号昇順で固定**
- **検証方法**: 45問目の回答で `#/result` へ／途中でリロードして同じ問・同じ回答が復元される／1問目で「戻る」が無効／375px幅で設問文と選択肢が読める／選択肢のタップ領域が44×44px以上／**出題順が毎回同一であることのテスト**（隣接する2問が同じ尺度にならないことも検証）
- **対応**: F-001、F-002、非機能（対応端末）

### T-009 採点・個人内標準化（F-003）
- **完了条件**: `processing-design.md` §1〜§3 のとおり。**母集団標準偏差（÷8）**。`sd < 1e-9` を判定不能とし、ゼロ除算を起こさない。同値は正準順で安定ソート
- **検証方法**: 全項目1・全項目5・全尺度同値で `standardizable = false` になり例外が出ない／既知の入力に対する z の期待値テスト／同値入力で順位が毎回同じ／標本SD（÷7）に変えるとテストが落ちる（実装固定の確認）
- **対応**: F-003、非機能（正確性）

### T-010 タイプ判定・僅差（F-004）
- **完了条件**: 上位2尺度から `typeId`。`|z[rank1] - z[rank2]| < 0.15` のとき `alternativeTypeId` を設定。判定不能時は `null`
- **検証方法**: 2位と3位が完全同値で代替タイプが出る／1位と2位を入れ替えても `typeId` が変わらない／閾値の境界値（0.1499 / 0.1501）の挙動／判定不能時に例外が出ない
- **対応**: F-004

### T-011 8尺度レーダー（F-005）
- **完了条件**: 正準順で固定の八角形レーダー。回転させない。判定不能時は素点でレーダーのみ描画する
- **検証方法**: 尺度の並びが `scale-definitions.js` の `order` と一致／375px幅でラベルが重ならない／canvas未対応時にテキストの数値一覧へフォールバックする
- **対応**: F-005

### T-012 結果文の組み立て（F-006）
- **完了条件**: タイプ名・説明・上位2尺度・下位1尺度の解説を、**そのまま表示できる完成文**として生成する。判定不能時は専用文
- **検証方法**: 禁止語（「平均より」「人より」「多数派」「少数派」「苦手」「向いていない」）が全28タイプ×代表パターンの出力に含まれないテスト／下位尺度の文が「相対的に低い」表現になっている
- **対応**: F-006、非機能（正確性・変更禁止事項2）

### T-013 S-003 結果画面
- **完了条件**: タイプ名（僅差時は2つ併記）、レーダー、解説、免責の常時表示、「カードを見る」導線。第2フェーズのロック予告枠を空けておく
- **検証方法**: 判定不能時に専用メッセージとレーダーのみが出て、カード画面へも進める／描画失敗時にテキストのみで結果が読める
- **対応**: F-003〜F-006、F-008

### T-014 出典・免責・データ削除（F-008）
- **完了条件**: S-005 に、医学的・心理学的診断ではない旨、ORVIS原論文とIPIPの出典、この端末のみに保存しサーバーへ送らない旨、全削除ボタン（確認ダイアログ付き）
- **検証方法**: 全削除後に初回状態へ戻る／削除をキャンセルしてデータが残る／免責が結果画面にも常時表示されている
- **対応**: F-008

### T-015 キャラクターアセット（F-019）
- **完了条件**: ハリネズミのポーズ8種＋小物8点。1024×1024、**背景透過（アルファ付き）**、文字・カード色を焼き込まない。`character-manifest.js` を `npm run character:manifest` で生成（sha256 integrity 付き）。altテキストを全件に持たせる
- **画風**【想定】: `assets-reference/` のスタイル参照画像は**素案**。精密な色鉛筆・図鑑風・中央に全身1体という方向性のみ確定。**小物の点数と構成は着手前に本人が決定する**
- **制作ルール**: 職業そのものを描かない（道具と所作に変換）／体格・針と尺度の優劣を結び付けない／**丸まった防御姿勢を使わない**（常に開いた姿勢）／猫との対立関係を連想させる配置を使わない
- **検証方法**: `npm run character:check` で全16点の存在・寸法・ハッシュ一致を検証／**全アセットにアルファチャンネルがあり四隅が透明であること**を機械的に検証（白背景のまま書き出す事故を防ぐ）／manifestに欠番がない／altが空でない／判定不能時に使う中立ポーズが用意されている
- **対応**: F-019

### T-016 カード生成（F-018）
- **完了条件**: canvasで、ポーズ＋小物＋タイプ名＋レーダー＋ORVISバッジ2点＋出典・免責を合成し、`toBlob()` で画像を得る。**自オリジンのアセットのみを使う**。縦横比を維持し、トリミングしない
- **検証方法**: 生成画像の縦横比が原寸と一致／画像の遅延読込失敗時にエラーを握りつぶさずテキスト結果へ戻す／`toBlob()` が null を返すケースの処理／28タイプ×代表パターンで文字がはみ出さない
- **対応**: F-018、非機能（画像表示）

### T-017 S-004 カード画面・共有（F-018）
- **完了条件**: プレビュー表示、「画像を保存」、「共有」。Web Share API 対応時は端末の共有メニュー、非対応時はURLコピーへフォールバック。**回答値・得点をURLに含めない**。成功・失敗を表示
- **検証方法**: Share API を無効化してURLコピーへ落ちる／Clipboardも使えない環境で選択可能テキストに到達できる／共有URLにクエリ・フラグメントで得点が含まれない／失敗しても結果画面へ戻れる
- **対応**: F-018、非機能（共有）

### T-018 フォント・アイコンの確定
- **完了条件**: S-001・S-002・S-003 のモックを候補フォント3種で並べて比較し、`docs/screens.md` のデザイントークンを確定（【想定】を外す）。アイコンはインラインSVGで統一し、ライブラリを混在させない
- **検証方法**: 実画面のスクリーンショットを375px・1280pxで取得して比較／確定後に `docs/screens.md` の状態列が「確定」になっている
- **対応**: 非機能（UI表現）

### T-019 タイプ命名表28件（コンテンツ）
- **完了条件**: 28件すべてに名前（日本語の名詞句）と説明2〜3文。完全同一の名前がない。末尾の役割語の重複は許容するが、名前全文で識別できる
- **検証方法**: 28件の重複チェックのテスト／禁止語テスト（T-012と同じ辞書）／能力・優劣・職業の貴賤に読まれる表現がないことのレビュー
- **対応**: F-004

### T-020 端値テスト（正確性）
- **完了条件**: `processing-design.md` §10-1 の8ケースがテストとして存在し、すべて通る
- **検証方法**: `npm test` に含まれる／意図的に `sd == 0` の分岐を消すとテストが落ちる
- **対応**: 非機能（正確性）

### T-021 PC・スマホのスモーク
- **完了条件**: 375px と 1280px で S-001→S-002→S-003→S-004 を完走できる
- **検証方法**: 両viewportの実画面スクリーンショットを取得し、横スクロールが発生しないこと・固定要素が設問を隠さないことを確認
- **対応**: 非機能（対応端末）

### T-022 外部通信ゼロの検証（セキュリティ）
- **完了条件**: MVPでは自オリジン以外へ通信しない。解析タグ・フォントCDN・エラー収集を入れない
- **検証方法**: `fetch`／`XMLHttpRequest` をスタブ化し、自オリジン以外の呼び出しがゼロであることをテスト／HTMLに外部ドメインの `src`・`href` がないことを静的チェック
- **対応**: 非機能（セキュリティ）

### T-023 日時・タイムゾーンの検証
- **完了条件**: 保存する日時はすべて UTC の ISO 8601。表示は環境タイムゾーンに追従し、取得不可なら `Asia/Tokyo`。日付見出しは JST のローカル日付で判定
- **検証方法**: CI を `TZ=UTC` で実行して通る／`TZ=Asia/Tokyo` でも通る／JST 0時前後・月末月初・UTCとJSTで日付が変わる時間帯（JST 00:00〜09:00）の境界テスト／固定オフセット文字列が保存値に混入しないテスト
- **対応**: 非機能（日時）

## 第2フェーズ（連携と掛け合わせ）

| ID | タスク | 完了条件 | 検証方法 | 対応 |
|---|---|---|---|---|
| T-025 | 結果コードの受け取り | **完了（2026-09-05）**。`domain/big-five-link.js` で検証、`infrastructure/linkage-intake.js` で受け取りと保存。版数違い・桁数違い・値域外・全角・空文字はいずれも無視して単体結果。**成否によらずフラグメントを消す。**ページ読み込みとハッシュ変更の両方の経路に通した。`appMeta.linkageEnabled` がキルスイッチ | 実ブラウザで確認済み | F-010 |
| T-026 | 後付け連携 | **完了（2026-09-05）**。`attachBigFive()` が `bigFive` だけを差し替え、`replaceLatestResult()` が件数を増やさずに最新の結果を更新する。先に連携してから45問に答えた場合も結果に乗る | 実ブラウザで確認：同じ resultId のまま連携が付き、結果件数も進捗も変わらない | F-011 |
| T-027 | ②整合／不整合と③固有性 | **完了（2026-09-05）**。`domain/cross-analysis.js` と `domain/cross-analysis-text.js`。②は**並置型**（processing-design §8-1）。本人の2つの事実と原典で報告された強さを別々に書き、推論は読み手に返す。閾値 `|z|≧0.3`、強さは揃った＝`min(|z|)`／分かれた＝`|gap|`、同じ因子・尺度は1回まで、最大2件。6ペアすべて落ちても1件出す（変更禁止事項4）。定型文は6ペア×3本＝18本（揃ったは両方高い／両方低いで別）。**§10-2のゲートは「言い切りの強さ」へ変更**したので、友人サンプルを待たずに出せる。③は従来どおり手仕事・挑戦が上位2位のときだけ | 実例（協調性×支援）で1件／2件／0件、重複排除、決定的であること、18本の網羅と断定回避、禁止語、位置づけの語をテスト。実ブラウザで②2件（揃った・分かれた）を確認 | F-012, F-013 |
| T-028 | ロック予告 | **完了（2026-09-05）／v1.21で改訂**。`lockPreview()`。**②が入ったので全員へ出す**（③が出ない人にも渡せるものがあるため）。**件数も内容も約束しない**（連携前は相手の5因子が無い）。**「判定」の語を使わない**（出典・免責の「判定するものではありません」と衝突する）。③の対象がある人には、従来どおり本人の順位と領域名を名指しする文を後ろに足す | ③が無い人にも出る／件数を約束しない／「判定」が入らない／③の予告と本文が一致する／判定不能なら出さない | F-014 |
| T-029 | 相手の因子バッジ | **完了（2026-09-05）**。`domain/partner-badges.js`。**記号19点をやめ、文字で組む**（本人決定）。3.0から離れている順に最大2因子、該当なしは「バランス」1枚。表記は**因子名＋0〜100の表示値**で、「高い／低い」という評価の語をカードに出さない。表示値の式はココロパレアの `displayScoreFromRational` と同値で、項目数（10問／4問）によらず一致することを確かめた。座標は `card-layout.js` の `reservedBand.badge` | 帯の内側に収まる／最長の2つでも幅800に収まる／未連携なら描かない／境目3.5・2.5の扱い／表示値7点（1.00→0〜5.00→100）／文字に高低の語が入らない | F-020 |
| T-030 | 合成カード | **完了（2026-09-05）**。3枚目を作らず、**連携済みのとき同じカードに猫を加える**（D-20）。猫＝むっくんの75%・実体どうし6px・接地線+34px。座標は `card-layout.js` の `guest`。アセットは自オリジン（`app/assets/guest/`）。切り替えは自動で、判断材料は `snapshot.bigFive` | 連携ありで猫が出る／なしで出ない／判定不能でも出る／間隔が0以上であることをテスト。8ポーズ全件を実物で確認 | F-022 |
| T-031 | LLM推敲 | **【不要】**（2026-09-05 本人決定）。実装しない。`appMeta.llmEndpoint` は null のまま、外部通信ゼロを維持する | — | F-007 |
| T-032 | ココロパレア側改修 | **完了（2026-09-05・Codex）**。あちらの `main` にマージ済みで、本番（`https://kokoro.sikumilab.com/`・mvp-1.1.1）にも反映済み。`domain/sigotosocket-link.js` が `v1-`＋15桁のURLを組み立て、`result-screen.js` の操作群に「シゴトソケットへ結果を渡す」を1つ置く。**`mode === "detail50"` のときだけ**出す。遷移は `location.href`（同じタブ）。組み立てに失敗したら結果画面に残して知らせる。**仕様書（`docs/連携仕様_ココロパレア側の実装.md`）どおりに実装されていることを確認した。**F-021（相手の称号名）と F-023（相互表示）は**入れていない**（v1コードは称号IDを運ばない・§6-4） | こちらのトップから「準備中」の断りを外し、渡せる場所の案内に差し替えた | F-021, F-023 |
| T-035 | α実測スクリプト | **完了（2026-09-05）**。`npm run sample:template` で見出し行、`npm run sample:check <CSV>` で尺度別αと6ペアの符号を出す。**件数を隠さない**（n・但し書きを必ず添える）。使えない行は落として理由を出し、他の行は活かす | 手計算できるデータでα=1・相関±1を検証。合計が動かない／定数列は null（ゼロ除算にしない） | 非機能（正確性） |
| T-036 | 結果画面のキャラクター | **完了（2026-09-05）**。`presentation/character-figure.js`。1位のポーズ＋2位の小物を、**カードと同じ組み合わせ・同じ比率**で出す（比率は `card-layout.js` から取り、直書きしない）。判定不能でも `neutral` で出す。画像が出せなければ説明文へ退避し、読み上げも描けたものだけを言う。**寸法は style 属性ではなく CSSOM で入れる**（`index.html` の CSP が `style-src 'self'` なので style 属性は無視される） | ポーズ／小物が出る・出ない、判定不能で neutral、比率が card-layout と一致、style属性を使っていない、説明文が残ることをテスト。実ブラウザで確認 | F-019 |
| T-037 | トップの見出し | **完了（2026-09-05）**。「やってみたいことのかたちを見る」→**「やってみたいことを知る」**（本人決定）。OGPのコピーは変えていない（`docs/brand.md`・`index.html` の og:description は従来のまま） | 見出しの文言をテスト | 非機能（文言） |
| T-038 | 開いたままのタブでの連携 | **完了（2026-09-05）**。`hashchange` で `receiveBigFive()` が受け取れたとき、`store.latestResult()` をメモリへ読み直す。**これが無いと、タブを開いたまま連携リンクを踏んでも、結果画面とカードに連携が出ない**（保存は更新されているのに画面が古い状態を見続ける）。起動時の経路は受け取りのあとに読み込むので影響しない | **単体テストは置いていない。**main.js の配線であり、部品を切り出しても「呼び忘れ」は捕まらない。実ブラウザで、未連携の結果画面 → `#b5=` → リロードなしで②が出ることを確認した | F-011 |

## 第3フェーズ

| ID | タスク | 対応 |
|---|---|---|
| T-033 | 診断履歴 | F-015 |
| T-034 | IPIP-VIA-R（第3診断） | F-017 |

## トレーサビリティ表

| 機能ID | 状態 | 画面 | 処理 | データ | タスク |
|---|---|---|---|---|---|
| F-001 | MVP | S-002 | — | 項目マスタ, ResponseState | T-003, T-008 |
| F-002 | MVP | S-001, S-002 | — | ResponseState | T-005, T-007, T-008 |
| F-003 | MVP | S-003 | §1〜§3 | ScaleScore | T-009 |
| F-004 | MVP | S-003 | §4 | type-definitions | T-004, T-010, T-019 |
| F-005 | MVP | S-003 | — | ScaleScore | T-011 |
| F-006 | MVP | S-003 | §6 | 結果文定義 | T-012 |
| F-007 | 第2 | S-003 | §7 | — | T-031 |
| F-008 | MVP | S-003, S-005 | — | — | T-014 |
| F-009 | MVP | 全画面 | — | app-meta | T-002 |
| F-010 | 第2 | S-003 | api §1 | BigFiveLink | T-025 |
| F-011 | 第2 | S-003 | — | ResultSnapshot | T-026 |
| F-012 | 第2 | S-003 | §8 | BigFiveLink | T-027 |
| F-013 | 第2 | S-003 | §9 | BigFiveLink | T-027 |
| F-014 | 第2 | S-003 | — | — | T-028 |
| F-015 | 第3 | — | — | results[] | T-033 |
| F-016 | **【失効】** | — | — | — | **F-018へ統合** |
| F-017 | 第3 | — | — | — | T-034 |
| F-018 | MVP | S-004 | — | cardTemplateVersion | T-016, T-017 |
| F-019 | MVP | S-004 | §5 | character-manifest | T-015 |
| F-020 | 第2 | S-004 | — | バッジ定義 | T-029 |
| F-021 | 第2 | S-004 | — | BigFiveLink.titleId | T-029, T-032 |
| F-022 | 第2 | S-004 | — | guest asset | T-030 |
| F-023 | 第2 | （ココロパレア） | — | — | T-032 |
| F-024 | MVP | S-003, S-004 | — | scale-definitions.hollandType | T-024 |
| 非機能・性能 | MVP | — | — | — | T-009, T-031 |
| 非機能・セキュリティ | MVP | — | — | — | T-005, T-022, T-031 |
| 非機能・運用 | MVP | — | — | — | T-001, T-002 |

| 非機能・正確性 | MVP | — | §10 | item-master | T-003, T-009, T-020, T-035 |
| 非機能・対応端末 | MVP | 全画面 | — | — | T-008, T-021 |
| 非機能・画像/共有 | MVP | S-004 | — | — | T-016, T-017 |
| 非機能・日時 | MVP | — | — | — | T-023 |
| 非機能・UI表現 | MVP | 全画面 | — | — | T-018 |

**空欄なし。** F-016 は【失効】のため置換先（F-018）のみを追う。
F-024 は v1.8 で採番したが表への追加が漏れていた（2026-09-05 に補記）。

## 主要異常系

| 異常 | サーバー動作 | クライアント動作 | 冪等性 | 回復方法 | テスト |
|---|---|---|---|---|---|
| localStorage 書込失敗 | — | メモリ動作へフォールバックし通知 | 保存は冪等 | 継続利用可。結果はその場で表示 | T-005 |
| schemaVersion 不一致 | — | 読み捨てず保持、移行不能を表示 | — | 全削除で初期化 | T-005 |
| 全尺度が同値 | — | 判定不能メッセージ＋レーダーのみ | — | 再診断 | T-009, T-020 |
| canvas / toBlob 失敗 | — | テキスト結果へ戻す | 生成は冪等 | 結果画面へ | T-016 |
| Web Share 非対応 | — | URLコピーへフォールバック | — | 選択可能テキスト | T-017 |
| 結果コード不正（第2） | — | 無視して単体結果 | — | 再度連携 | T-025 |
| LLM停止・タイムアウト（第2） | — | 組み立て済みの文を表示 | 冪等 | 自動 | T-031 |
| 二重送信（次へ連打） | — | ページ遷移を1回に抑制 | — | — | T-008 |
