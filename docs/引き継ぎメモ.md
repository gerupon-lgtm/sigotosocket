# 引き継ぎメモ

**Claude Code など、別のセッションでこの続きをやる人向け。**
会話の文脈は引き継がれない。文書とテストとコミット履歴だけが根拠になる。

作成 2026-09-05。

---

## 1. 最初に読むもの（この順で）

| 順 | ファイル | 何が書いてあるか |
|---|---|---|
| 1 | `AGENTS.md` | **変更禁止事項10項目とその根拠。**「改善」として書き換えないこと |
| 2 | `docs/tasks.md` | 進め方の合意、タスクの状態、①②③の工程 |
| 3 | `docs/requirements/要件定義書_シゴトソケット.md` | 要件と改訂履歴（v1.15）。§7-3 §11-0 は今日決めた分 |
| 4 | `docs/brand/card-layout.md` | カードのレイアウト確定値と、その決め方 |
| 5 | `../構想たたき台_シゴトソケット.md` | 決定ログ D-01〜D-19（追記専用・上書き禁止） |

## 2. いまどこまで進んでいるか

**MVPの実装は完了。**残るのは下の「3. 残作業」＝実機確認・push・第2フェーズの3つだけ。

- 診断ロジック（採点・個人内標準化・タイプ判定・結果文）は完了、テスト済み
- カードは1080×1800で刷新済み。座標は `app/js/presentation/card-layout.js` が正典
- キャラクターアセット18点は変換して組み込み済み。目録（sha256・alt）も生成済み
- 称号の明朝はサブセットを同梱済み
- ブランド一式（マーク・アイコン・OGP・CSP）は完了
- テスト105件、静的検査・版数・項目マスタ・アセット検査すべて通過

### 2026-09-05 に閉じた分

| 何を | どこ |
|---|---|
| ホランド型を結果画面へ接続 | `result-screen.js`。判定不能のときは節ごと出さない |
| 称号の明朝をサブセット同梱 | `npm run fonts:build` → `app/assets/fonts/`。161字・38.9KB |
| キャラクターの目録 | `npm run character:manifest` → `app/js/data/character-manifest.js` |
| 判定不能のカードに中立ポーズ | 「準備中」の枠が出るのは読み込み失敗時だけになった |

**引き継ぎメモの「151字」は足りなかった。**明朝で描くのは称号28件（145字）だけでなく、
「あなたの称号」「45問 詳細結果」の2つのピルと「称号を決められませんでした」も含む。
合わせて161字。何の字が要るかは `app/js/presentation/mincho-coverage.js` が唯一の根拠で、
`card-renderer.js` で明朝を使う箇所を増やしたらそこにも足す（足し忘れはテストが落とす）。

### 進め方の合意（守ってほしい）

**一度に1つだけ閉じる。要素 → レイアウト → 実装の順で、混ぜない。**
この合意に至った経緯は `docs/tasks.md` の「混ぜて進めて失敗した記録」にある。

## 3. 残作業（優先順）

1. **実機確認（T-021）** — **これは人がやるしかない。**
   ブラウザ幅での確認（375×812 / 1280×900）は済んでいる。横スクロール0px、タップ領域は最小53px、
   `toBlob()` は478KBのPNGを返す。判定不能の経路も通した。**残るのは実端末での確認**で、
   とくに iOS Safari の Web Share（画像付き共有）と、実機でのフォントの見え方
2. **GitHubへのpush** — リモートは設定済み、未push。Pages側の設定（Settings → Pages → Source を GitHub Actions）が必要。
   **CIは `npm ci` を実行しない**（node_modules 無しで `npm test` を走らせる）。
   テストが依存パッケージを読まない状態を保つこと
3. **第2フェーズ（連携）** — **着手前に本人の許可を取ること。**約束事項

### 未決のまま残っているもの

- **`character-pose-guest.webp` は猫ではなくハリネズミ。**要件では F-022 のゲストは猫1図柄。
  むっくんがココロパレア側へゲスト出演する図（F-021/F-023）なのか、置き場所を間違えたのかが判別できない。
  第2フェーズに入る前に本人へ確認すること。目録には入れてあるがMVPのカードには出さない

## 4. 落とし穴（実際に踏んだもの）

**同じ穴を掘り返さないための記録。**

### sharp の処理順序
`extract → resize → extend` の順で適用される。`extend` で正方形化しようとすると
**リサイズ後**に効いて寸法が狂う（1124×1024になった）。`fit: "contain"` で一度に行う。

### キャラクターアセットの余白
1枚ずつ余白を切ってはいけない。ポーズの縦横比は 0.75〜1.05 とばらついていて、
1枚ずつ切ると立ち姿と伏せ姿が同じ高さに正規化される。
`npm run character:build` は**全種の実体を包む共通矩形**で切る。

### モックと実装は別物
`scripts/brand/build-card-full.mjs` は検討用のモックで、SVG＋sharpで描いている。
実装はcanvas。**特に縁取りはモックの見え方と実装が異なる。**モックの数値をそのまま使わない。

### 文字被りのテストには穴が空きやすい
`card-layout.test.js` の「重なって当然の組」は**ピルとその中の文字だけ**。
一度「称号ピル|称号」を許可してしまい、称号を60px動かしても検出できない状態になっていた。
**許可リストを増やすときは、座標をずらす変異テストで実際に落ちることを確認すること。**

### 決定ログは追記専用
一度、文字列置換で D-01 の本文を書き換えて履歴を壊した。
古い決定は**本文を残したまま `【失効】` の印を付す**。

### 幅の見積もり
和文は原則全角なので「文字数 × フォントサイズ」を上限として使える。
ラテン文字・数字は半角。実行環境の書体差を吸収する安全側の見積もり。

## 5. 検証コマンド

```
npm test              # 105件
npm run check         # 外部通信ゼロ
npm run version:check # 版数の同値
npm run items:check   # 項目マスタ45問
npm run character:check  # アセット18枚
npm run brand:icons   # アイコンとOGPの再生成
npm run character:build <入力> <出力>  # 元画像から配布用webp
npm run character:manifest # アセットの目録（sha256・alt）を再生成
npm run fonts:build [元フォント]  # 称号の明朝サブセットを再生成
```

`fonts:build` と `character:manifest` は `node_modules` が要る。生成物はgit管理しているので、
**普段は実行しなくてよい。**称号やアセットを変えたときだけ実行する。実行し忘れはテストが落として知らせる。

## 6. 触るときに注意するもの

- **ココロパレア（`C:\Users\user\Documents\診断系アプリ開発`）は勝手に変更しない。**参照のみ
- **第2フェーズ（連携）は着手前に許可を取る**
- `app/js/data/item-master.js` は生成物。手で編集せず `npm run items:build`
- `app/js/data/character-manifest.js` も生成物。altを直すのは `scripts/characters/character-alt.js`
- `app/assets/fonts/` も生成物。称号を足したら `npm run fonts:build`
- カードの座標を `card-renderer.js` に直書きしない。`card-layout.js` へ

## 7. 文書に残っていない前提

会話でしか触れていないもの。必要なら本人に確認すること。

- **カードのサブドメインは未確定**（要確認21）。`appMeta.siteOrigin` に仮値が入っている
- 縁取りの具体値（太さ・ぼかし）は「細く柔らかく」という方針のみ。実装時に実物で決める
- キャラクターの枠取り（リースのような装飾）は**なしで確定**。装飾アセットは作っていない
- ココロパレアのカードは1080×1800で、こちらはそれに合わせた。あちらは5因子バーとアロマ3枠で
  下半分が埋まるが、こちらは埋めるものがなく余白が多い。これは既知で、許容している
